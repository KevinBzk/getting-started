#Tuples are like lists but cant be changed or cant be adjusted

#you do a tuple like a list but instead of a [] you use a ()

tuple = (0,1,1,4,6,4,3)


#The funktions are the same as the funktions for the list but you cant use funktion to adjust the tuple

print(tuple.count(1))
print(tuple[4])